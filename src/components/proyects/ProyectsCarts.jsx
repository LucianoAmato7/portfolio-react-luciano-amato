import { Link } from "react-router-dom";

const ProyectsCarts = ({ name, image, url, techs }) => {
  return (
    <Link to={`${url}`} target="_blank">
      <div>

      </div>
    </Link>
  );
};

export default ProyectsCarts;